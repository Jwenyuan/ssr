import Home from '../modules/home';
import Page from '../modules/page';

const routes = [
    {
        path: '/home',
        component: Home
    },
    {
        path: '/page',
        component: Page
    }
];

export default routes;
